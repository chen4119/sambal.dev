{
    "name": "sambal.dev",
    "version": "0.0.1",
    "description": "Sambal website",
    "author": "Wan Chun Chen",
    "license": "MIT",
    "engines": {
        "node": ">=10.0.0"
    },
    "scripts": {
        "start": "sambal serve",
        "build": "sambal build",
        "deploy": "npm run theme && npm run build",
        "theme": "cd sambal-ui-material && npm install && npm run theme && cd .."
    },
    "devDependencies": {
        "sambal": "0.0.42"
    }
}
